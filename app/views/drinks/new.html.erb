<h1 class='title'>Register Drink</h1>

<% if @drink.errors.any? %>
  <ul>
    <% @drink.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>