<h2><%= @drink.name %> (<%= @drink.status.humanize %>)</h2>

<%= link_to 'Price History', establishment_drink_drink_price_history_index_path(@establishment.id, @drink.id) %>

<% if @drink.status == 'active' %>
  <%= button_to 'Deactivate', inactive_establishment_drink_path(@establishment.id, @drink.id) %>
<% else %>
  <%= button_to 'Activate', active_establishment_drink_path(@establishment.id, @drink.id) %>
<% end %>

<section>
  <div class='img'><%= image_tag @drink.image %></div>
  <p><%= @drink.description %></p>
  <p><%= @drink.calories %> cal</p>
  <p>Is alcoholic? <%= @drink.is_alcoholic.humanize %></p>
</section>

<%= link_to 'Edit', edit_establishment_drink_path(@establishment.id, @drink.id) %>

<%= button_to 'Delete', establishment_drink_path(@establishment.id, @drink.id), 
    method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } } %>

<h2>Options</h2>

<div><%= link_to 'Register Drink Option', new_establishment_drink_drink_option_path(@establishment.id, @drink.id) %></div>

<% if @drink.drink_options.any? %>
  <ul>
    <% @drink.drink_options.each do |option| %>
      <li><%= option.description %></li>
      <li><%= number_to_currency(option.price, unit: "R$", separator: ",", delimiter: ".") %></li>

      <%= link_to 'Edit', 
          edit_establishment_drink_drink_option_path(@establishment.id, @drink.id, option.id), 
          id: dom_id(option, :edit) %>
    <% end %>
  </ul>
<% else %>
  <p>There are no registered drink options</p>
<% end %>
