<h1 class='title'>Add Item</h1>

<% if @order_dish.errors.any? %>
  <ul>
    <% @order_dish.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>