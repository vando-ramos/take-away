<h1 class='title'><%= t('views.new_dish_option') %></h1>

<% if @dish_option.errors.any? %>
  <ul>
    <% @dish_option.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>