<h1 class='title'><%= t('actions.add_drink') %></h1>

<% if @order_drink.errors.any? %>
  <ul>
    <% @order_drink.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>