<h1 class='title'><%= t('views.new_dish') %></h1>

<% if @dish.errors.any? %>
  <ul>
    <% @dish.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>