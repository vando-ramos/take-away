<h1 class='title'>Register Dish</h1>

<% if @dish.errors.any? %>
  <ul>
    <% @dish.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>